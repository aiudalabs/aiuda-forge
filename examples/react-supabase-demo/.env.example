# Copy to .env.local and fill from `supabase start` output (or your project's
# API settings). The browser holds ONLY these two — never the service_role key.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-anon-key
